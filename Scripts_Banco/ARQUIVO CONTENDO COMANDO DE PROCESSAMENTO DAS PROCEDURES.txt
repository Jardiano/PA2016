-- Sintaxe para Processamento das Procedures no PL SQL
DECLARE TESTEPROCEDURE INT;
BEGIN
    SELECT VL_PARAMETRO INTO TESTEPROCEDURE FROM PARAMETRIZACAO WHERE ID_PARAMETRO_SISTEMA = 1;
    SP_PROCESSADADOSITEM_TODOS(TESTEPROCEDURE);
    SP_PROCESSADADOSGRUPO_TODOS(TESTEPROCEDURE);
END;
